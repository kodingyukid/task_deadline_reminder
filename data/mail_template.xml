<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <record id="email_template_task_deadline_reminder" model="mail.template">
            <field name="name">Task Deadline Reminder</field>
            <field name="model_id" ref="project.model_project_task"/>
            <field name="subject">Task Deadline Reminder: ${object.name}</field>
            <field name="email_from">${object.company_id.email or ''}</field>
            <field name="body_html"><![CDATA[
<p>Dear Task Assignee,</p>

<p>This is a friendly reminder that the following task is approaching its deadline:</p>

<p>
    <strong>Task:</strong> ${object.name}<br>
    <strong>Project:</strong> ${object.project_id.name}<br>
    <strong>Deadline:</strong> ${format_date(object.date_deadline)}
</p>

<p>Please ensure that the task is completed by the deadline. If you have any questions, please contact your project manager.</p>

<p>
    Thank you,<br>
    ${object.company_id.name}
</p>
            ]]></field>
        </record>
    </data>
</odoo>
